# CLAUDE.md

## 프로젝트 개요
- **프로젝트명**: 나의 포트폴리오
- **목적**: 디자인 작업물을 소개하는 개인 포트폴리오 사이트

## 핵심 기능
1. 자기소개
2. 작업물 목록
3. 작업물 상세
4. 로그인
5. 작업물 등록 / 수정 / 삭제 (CRUD)

## 현재 진행 상태
- 현재는 정적 HTML(`index.html`, `about.html`)과 Supabase 연동 미리보기(`preview.html`)로 기본 뼈대만 있는 초기 단계.
- `preview.html`은 Supabase `test_todos` 테이블을 목록으로 보여주는 연습용 페이지이며, 향후 "작업물 목록/상세/CRUD" 기능 구현 시 동일한 패턴(REST API 호출 → 목록 렌더링)을 참고해 확장할 예정.
- 로그인 기능은 아직 구현되지 않음. Supabase Auth 도입 시 이 문서를 함께 업데이트할 것.

## 기술 스택
- 정적 HTML / CSS / JS (프레임워크 미사용)
- 공통 스타일: `style.css` (블루-그린 톤 디자인 시스템)
- 데이터베이스: Supabase (REST API, `anon public` 키로 클라이언트에서 직접 호출)

## 작업 시 유의사항
- Supabase **비밀번호나 서비스 키(secret key)는 절대 커밋하지 않는다.** `.gitignore`에 관련 패턴이 등록되어 있음. `anon public` 키는 클라이언트 노출용이라 커밋해도 무방.
- 커밋 메시지는 한글로 작성한다.
- 새 페이지를 추가할 때는 `style.css`를 링크해 기존 디자인 톤(블루-그린 계열)을 유지한다.

## 폰트 규칙
- 폰트 컬러를 배경색과 안겹치게 잘 보이는 컬러로 설정.